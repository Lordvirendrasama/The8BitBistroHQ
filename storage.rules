rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allowing public read/write for development. 
      // In production, you would restrict this to authenticated users.
      allow read, write: if true;
    }
  }
}
